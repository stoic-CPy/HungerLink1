# Base URL of the deployed HungerLink API (no trailing slash), e.g.
# https://hungerlink-api.onrender.com/api
# For local development against the backend in this repo:
VITE_API_URL=http://localhost:4000/api
